import 'package:flutter/material.dart';

class PastPapers extends StatelessWidget {
  const PastPapers({super.key});

  @override
  Widget build(BuildContext context) {
    return Text('Past Papers');
  }
}
